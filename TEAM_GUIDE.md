# Гід команди — як користуватись ботом

Ця сторінка — для будь-кого в команді, хто просто спілкується з ботом
у Telegram. Технічні деталі (як усе влаштовано всередині) тут
навмисно не описані — дивіться README.md, якщо цікаво саме це.

Швидкий старт: напишіть боту `/help` — він одразу покаже повний
список команд.

---

## Задачі — щоденна робота

- **`/задача <текст>`** — додати задачу. Приклад: `/задача полагодити
  кнопку в календарі`.
- **`/задачі`** — показати всі відкриті задачі.
- **`/done <номер>`** — позначити задачу виконаною. Приклад: `/done 7`.
- Можна взагалі не використовувати команди — просто напишіть боту
  звичайним текстом ("треба купити каву для офісу"), і він сам
  зрозуміє, що це задача, і додасть її.

**Швидкий виклик агента прямо із задачі:** якщо на початку тексту
задачі написати тег, бот одразу передасть її відповідному агенту:

- `/задача @дизайн зроби мокап екрана профілю` → одразу малює мокап
- `/задача @багфікс кнопка "зберегти" не працює` → одразу заводить
  баг-репорт
- `/задача @деплой` → одразу запускає позачергову перевірку коду

---

## Дизайн — мокапи й пітчі

Пишіть у чат **дизайн-бота**:

- **`/дизайн <опис екрана>`** — бот намалює HTML-мокап і надішле
  файлом. Відкривається просто в браузері.
- **`/дизайн_презентація <опис ідеї>`** — коротка презентація нової
  ідеї (проблема / рішення / як працює / наступні кроки), файлом
  .pptx.

---

## Стендап

Пишіть у чат **стендап-бота**:

- **`/стендап`** — зведення: що зроблено, що відкрито, які PR
  чекають на підтвердження. Приходить і без запиту — щодня о певній
  годині автоматично.

---

## Ідеї

Пишіть у чат **ідея-бота**:

- **`/ідея_продукт`** — 5 ідей нових фіч.
- **`/ідея_контент`** — 5 ідей коротких відео (з реальним пошуком
  актуальних трендів, не вигадка з голови).
- Під кожною ідеєю є кнопка **"💾 Зберегти"** — тисніть, якщо ідея
  сподобалась.
- **`/ідеї_збережені`** — список усього, що ви зберегли.
- Раз на тиждень бот сам, без запиту, надсилає пачку свіжих ідей.

---

## Презентація по задачах

- **`/презентація`** — звіт по задачах команди за останні 7 днів,
  файлом .pptx. Можна вказати `/презентація 30` (за 30 днів) чи
  `/презентація all` (за весь час).

---

## Як затверджувати PR через кнопки

Коли бот-помічник (деплой чи багфікс) щось змінює в коді, він **не**
робить це наосліп — він надсилає повідомлення в чат із кнопками:

**✅ Затвердити** — і **❌ Відхилити**

Просто натисніть потрібну кнопку прямо в Telegram. Більше нічого
робити не треба — все інше бот зробить сам. Якщо не натиснути жодну
кнопку, зміна просто чекає — нічого само собою не станеться.

Виняток: зовсім дрібні й безпечні правки (форматування коду тощо)
бот іноді застосовує сам, без запитання — про це теж прийде
повідомлення, просто без кнопок (уже по факту).

Ще двоє ботів працюють у фоні й нічого від вас не вимагають:
- **Рев'ю-бот** — сам перевіряє код на очевидні помилки й лишає
  коментар на GitHub, просто для інформації.
- **Changelog-бот** — після кожної прийнятої зміни коротко пише
  "що нового" людською мовою.

---

## Статус — чи все живе

- **`/статус_агентів`** — показує, чи все працює: коли востаннє
  успішно спрацював деплой, коли востаннє надсилався стендап і
  ідеї, скільки відкритих PR чекають на вас, чи живий сам бот.
  Перш ніж запитувати когось "чи бот взагалі працює?" — спочатку
  просто напишіть цю команду.

---

## Бот довго не відповідає — що робити

1. Зачекайте 5-10 хвилин. У системи є "сторож", який сам помічає,
   якщо бот завис, і перезапускає його — це займає трохи часу.
2. Напишіть **`/статус_агентів`** — там видно, чи бот справді не
   відповідає, чи просто конкретна команда забарилась.
3. Якщо й після цього нічого не змінюється — напишіть тому, хто
   налаштовував систему, з коротким описом: що ви написали, і що (чи
   що не) відповів бот.

Панікувати не треба — жодна ваша задача чи дані нікуди не зникають,
навіть якщо бот тимчасово не відповідає.
